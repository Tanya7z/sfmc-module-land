# @sfmc-bds/module-land

Wave C official SFMC module: **land**（现代地产庄园 · 只租不卖租赁契约）。

## Develop

```bash
npm install
npm run typecheck
npm test
```

Install into platform:

```bash
sfmc mod install land --from dir:. --link
```
